# QB3: DCT *DEPRECATED*


The DCT branch was an experiment on applying QB3 as the entropy encoding step in JPEG compression. It works, but the results are considerably larger than the normal JPEG files, because there is very little similarity between the remninder values in the DCT frequency representation.
